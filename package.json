{
  "name": "@textactor/ner",
  "version": "0.1.2",
  "description": "TextActor NER",
  "main": "./lib/index.js",
  "types": "./types/index.d.ts",
  "scripts": {
    "pretest": "npm run clean && npm run tsc",
    "test": "ava --no-cache --verbose lib/**/*.test.js",
    "posttest": "npm run clean-test-code",
    "tsc": "tsc",
    "tscw": "tsc -w",
    "preversion": "npm run tsc",
    "postversion": "git push --follow-tags",
    "prepublishOnly": "npm run tsc && npm run clean-test-code",
    "clean": "rimraf ./lib && rimraf ./types",
    "clean-test-code": "rimraf ./lib/**/*.test.js && rimraf ./types/**/*.test.d.ts",
    "dev": "yarn remove @textactor/actor-domain && yarn link @textactor/actor-domain",
    "prod": "yarn unlink @textactor/actor-domain && yarn add @textactor/actor-domain"
  },
  "engines": {
    "node": ">=4.0.0"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/textactor/ner.git"
  },
  "keywords": [
    "textactor",
    "ner"
  ],
  "author": "Dumitru Cantea",
  "license": "ISC",
  "bugs": {
    "url": "https://github.com/textactor/ner/issues"
  },
  "homepage": "https://github.com/textactor/ner#readme",
  "devDependencies": {
    "@types/node": "latest",
    "ava": "^0.23.0",
    "rimraf": "^2.6.2",
    "typescript": "latest"
  },
  "dependencies": {
    "@textactor/actor-domain": "^0.1.1",
    "@textactor/domain": "^0.1.4",
    "concepts-parser": "^1.3.1"
  }
}
